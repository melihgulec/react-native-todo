import {StyleSheet} from 'react-native';
import Colors from '../../constants/Colors';

export default StyleSheet.create({
  input: {
    borderBottomWidth: 1,
    borderBottomColor: Colors.darkAqua,
    marginBottom: 15,
    color: Colors.white,
  },
});
